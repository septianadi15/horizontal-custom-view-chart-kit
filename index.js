/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';
import {name as appName} from './app.json';
import CharKit from './ChartKit';

AppRegistry.registerComponent(appName, () => CharKit);
